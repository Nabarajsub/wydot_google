apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: flash-cloud
spec:
  template:
    metadata:
      annotations:
        run.googleapis.com/startup-cpu-boost: "true"
        run.googleapis.com/cpu-throttling: "false"
    spec:
      serviceAccountName: cloud-run-sa@${PROJECT_ID}.iam.gserviceaccount.com
      containerConcurrency: 80
      timeoutSeconds: 900
      containers:
      - image: us-docker.pkg.dev/${PROJECT_ID}/apps/flash-cloud:latest
        env:
        - name: APP_FILE
          value: flash_cloud_2.5rpo.py
        - name: DOTENV_PATH
          value: /etc/secrets/.env
        - name: SECRETS_TOML_PATH
          value: /etc/secrets/streamlit/secrets.toml
        ports:
        - containerPort: 8080
        volumeMounts:
        - name: dotenv
          mountPath: /etc/secrets
          readOnly: true
        - name: streamlit-secrets
          mountPath: /etc/secrets/streamlit
          readOnly: true
      volumes:
      - name: dotenv
        secret:
          secretName: projects/${PROJECT_NUMBER}/secrets/shared-dotenv
          items:
          - key: "4"          # or "4"
            path: .env
      - name: streamlit-secrets
        secret:
          secretName: projects/${PROJECT_NUMBER}/secrets/streamlit-secrets-toml
          items:
          - key: "2"          # or "2"
            path: secrets.toml
  traffic:
  - percent: 100
    latestRevision: true
