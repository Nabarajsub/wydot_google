apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: wydot-chatbot
spec:
  template:
    metadata:
      annotations:
        run.googleapis.com/startup-cpu-boost: "true"
        run.googleapis.com/cpu-throttling: "false"
    spec:
      serviceAccountName: cloud-run-sa@${PROJECT_ID}.iam.gserviceaccount.com
      containerConcurrency: 80
      timeoutSeconds: 300
      containers:
      - image: ${IMAGE}
        env:
        - name: DOTENV_PATH
          value: /etc/secrets/.env
        - name: HF_HOME
          value: /app/model_cache
        - name: HF_HUB_OFFLINE
          value: "1"
        - name: TRANSFORMERS_OFFLINE
          value: "1"
        - name: CHAINLIT_FILES_DIRECTORY
          value: /tmp/.files
        - name: CHAINLIT_DB_FILE
          value: /tmp/chainlit.db
        - name: TELEMETRY_DB_PATH
          value: /tmp/telemetry.sqlite3
        - name: EVAL_DB_PATH
          value: /tmp/evaluation.sqlite3
        - name: HOME
          value: /tmp
        ports:
        - containerPort: 8080
        resources:
          limits:
            memory: "4Gi"
            cpu: "4"
        startupProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5
          failureThreshold: 20
        volumeMounts:
        - name: dotenv
          mountPath: /etc/secrets
          readOnly: true
      volumes:
      - name: dotenv
        secret:
          secretName: shared-dotenv
          items:
          - key: "2"
            path: .env
  traffic:
  - percent: 100
    latestRevision: true
